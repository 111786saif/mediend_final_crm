import TeamLeadPipelinePage from '@/app/team-lead/pipeline/page'

/** Reuses the existing Team Lead pipeline for Project Head (Executive Assistant). */
export default function EAPipelinePage() {
  return <TeamLeadPipelinePage />
}
