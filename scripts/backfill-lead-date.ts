/**
 * OBSOLETE — superseded by the lead_date -> assigned_date rename (migration
 * 20260506120000_rename_lead_date_to_assigned_date). The "leadDate" column no
 * longer exists; the MySQL Lead_Date value is now stored in "assignedDate" and
 * is populated by the sync directly. This file is kept only for git history.
 */
export {}
