# Archived incremental migrations

These 78 migration folders were replaced by a single squashed init migration:

`prisma/migrations/20260710120000_init/migration.sql`

They are kept for historical reference only. **Do not** copy them back into `prisma/migrations/` — fresh production deploys use the init migration + `prisma migrate deploy`.

If you need to inspect how a specific schema change was introduced, search this archive by folder name.
